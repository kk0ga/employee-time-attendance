---
applyTo: "src/**/*.{ts,tsx,js,jsx}"
---

# React SPA 実装指針（勤怠）

## UI/状態
- 打刻は二重送信防止（押下→ローディング→完了までボタン無効）
- 当日未出勤なら退勤不可など、状態遷移をUIで防ぐ
- 監視画面は「最終更新時刻」を必ず表示し、失敗時は再試行導線を出す

## エラー処理
- 401/403: 再ログイン案内 or 管理者へ連絡導線
- 429/503: 指数バックオフでリトライ（上限あり）、ユーザーには「混雑中」表示
